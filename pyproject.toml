[project]
name = "cw-firmware-docs"
version = "0.1.0"
description = "Companion documentation site for the CW firmware projects"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "zensical>=0.0.44",
]
